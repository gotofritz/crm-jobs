[tool.poe.tasks]
dev = "python manage.py runserver"
test = "pytest"
lint = "ruff check ."
format-check = "ruff format --check ."
type-check = "pyright"
qa = ["lint", "format-check", "type-check", "test"]
